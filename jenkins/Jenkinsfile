pipeline {
    agent  any
    stages {
        stage('build') {
            steps {
                echo  'Buildings'
            }
             post {
                 always {
                     jiraSendBuildInfo site: 'magdeline22.atlassian.net', branch: 'TEST-123-awesome-feature'
                 }
             }
        }
    }
}
