pipeline {
    agent any 
   tools{
       maven(M3) 'Apache Maven 3.3'
   }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
