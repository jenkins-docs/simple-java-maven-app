pipeline {
    agent any
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Build-babu-rao') {
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
        stage('Test-baba') {
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
        stage('Deliver-package-yao') { 
            steps {
                sh './jenkins/scripts/deliver.sh' 
            }
        }
    }
}
