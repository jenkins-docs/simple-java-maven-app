pipeline{

agent{
	 node{
	   label 'slave2'
	 }
 }
 tools{
   maven 'maven385'
 }
 stages{
   stage("maven test"){
    steps{
	 sh 'mvn test'
	 }
	}
	stage("maven build"){
		steps{
		 sh 'mvn clean package'
		 }
	}
 }
 
}
