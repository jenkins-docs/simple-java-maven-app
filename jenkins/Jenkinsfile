pipeline {
    agent {
       label 'android-poc-slave'
          }
    stages {
        stage('Build') {
            steps {
                sh 'mvn -B -DskipTests clean package'
            }
        }
        stage('Test') {
              steps {
        script {
        //    steps {
          //      sh 'mvn test'
          //  }
         //   post {
         //       always {
         //           junit 'target/surefire-reports/*.xml'
         //       }
         //  }
            try {
        // Any maven phase that that triggers the test phase can be used here.
        sh 'mvn test'
        junit 'target/surefire-reports/*.xml'

    } catch(err) {
                junit 'target/surefire-reports/*.xml'
                throw err
    }
        }}
        }
        stage('Deliver') {
            steps {
                    echo "Im deploying now!"
                  archiveArtifacts artifacts: 'target/*.jar', fingerprint: true
                sh './jenkins/scripts/deliver.sh'
            }
        }
    }
    post {
        always {
  //          archiveArtifacts artifacts: 'target/*.jar', fingerprint: true
  //                  junit 'target/surefire-reports/*.xml'
slackSend baseUrl: 'https://jenkinsapp.slack.com/services/hooks/jenkins-ci/', channel: 'Dina Gamal', color: ' #439FE0', failOnError: true, message: 'build java pipeline', teamDomain: 'https://jenkinsapp.slack.com', token: 'L977N3yGsGjyJVpIGaqmXlHv'
        }
    }
}
