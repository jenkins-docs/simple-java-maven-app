pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                echo 'checking out from  git'
                git 'https://github.com/Francis-88-cpu/simple-java-maven-app.git'
            }
        }
        stage('compile') {
            steps {
                echo 'compiling'
                sh 'mvn compile'
            }