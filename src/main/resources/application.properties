# Database connection settings
db.url=jdbc:mysql://localhost:3306/mydb
db.username=root
db.password=password

# Application settings
app.name=Java Maven Tomcat Application
app.version=1.0.0

# Logging settings
logging.level=INFO
logging.file=logs/app.log

# Server settings
server.port=8080