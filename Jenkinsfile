pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build process'
            }
        }
        stage('Clean') {
            steps {
                echo 'Clean'
            }
        }
    }
}
