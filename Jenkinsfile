@Library('javapipeline-helpers') _

pipeline {
    agent any
    stages {
    stage ('Example') {
        steps {
             script { 
                // log.info 'Starting'
                 runpipeline ('karthi')
               //  log.warning 'Nothing to do!'
                 }
            }
        }
    }
}

