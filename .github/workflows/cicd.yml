name: Java CI with Maven

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Install Maven 3.9.2
      run: |
        MAVEN_VERSION=3.9.2
        wget https://archive.apache.org/dist/maven/maven-3/$MAVEN_VERSION/binaries/apache-maven-$MAVEN_VERSION-bin.tar.gz
        tar xzvf apache-maven-$MAVEN_VERSION-bin.tar.gz
        sudo mv apache-maven-$MAVEN_VERSION /opt/maven
        sudo ln -sf /opt/maven/bin/mvn /usr/bin/mvn
      env:
        JAVA_HOME: ${{ steps.setup-java.outputs.java-home }}

    - name: Verify Maven version
      run: mvn -version

    - name: Update Maven version
      id: update_version
      run: |
        VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
        PATCH=$(echo $VERSION | cut -d'.' -f3)
        NEW_PATCH=$((PATCH+1))
        NEW_VERSION=$(echo $VERSION | sed "s/$PATCH\$/$NEW_PATCH/")
        echo "new_version=$NEW_VERSION" >> $GITHUB_ENV
        echo "NEW_VERSION: $NEW_VERSION" # Debugging line to check the value of NEW_VERSION

    - name: Build with Maven
      run: mvn -B package --file pom.xml

    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: my-app-${{ env.new_version }}
        path: target/*.jar

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push Docker Image
      id: docker_build
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/simple-java-maven-app:${{ env.new_version }}

    - name: Commit and push new version
      if: success()
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git commit -am "Update version to ${{ env.new_version }}"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Deploy to EC2
      env:
        EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
        EC2_HOST: ${{ secrets.EC2_HOST }}
        NEW_VERSION: ${{ env.new_version }}
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      run: |
        mkdir -p ~/.ssh
        echo "${EC2_SSH_KEY}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H ${EC2_HOST} >> ~/.ssh/known_hosts
        ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ubuntu@${EC2_HOST} << 'EOF'
          echo "Pulling Docker image: ${DOCKER_USERNAME}/simple-java-maven-app:${NEW_VERSION}"
          docker pull ${DOCKER_USERNAME}/simple-java-maven-app:${NEW_VERSION}
          if [ $(docker ps -q -f name=my-app) ]; then
            echo "Stopping and removing existing container"
            docker stop my-app && docker rm my-app
          else
            echo "No existing container to stop and remove"
          fi
          docker run -d --name my-app -p 80:8080 ${DOCKER_USERNAME}/simple-java-maven-app:${NEW_VERSION}
        EOF